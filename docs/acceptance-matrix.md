# 受入テスト対応表(§21)

仕様書§21の各項目が、どの自動テストで検証されているかの一覧です。

- ドメインテスト: `npm test`(Vitest / 183件)
- ブラウザテスト: `npm run test:e2e`(Playwright / 65件)

## §21.1 重複・簡易照会

| 仕様の項目 | 検証しているテスト |
| --- | --- |
| 東都ホテル株式会社と東都ホテル、電話・Web空欄でsimilar | `duplicate.test.ts` 「東都ホテル株式会社と東都ホテルは、電話・Web空欄でも similar」 / `seed.test.ts` 「東都ホテル(電話・Web空欄)は similar」 / `duplicate-flow.spec.ts` 「東都ホテル(電話・Web空欄)は重複の可能性ありになる」 |
| 旧company weight 40／threshold 50でも完全一致はsimilar | `duplicate.test.ts` 「旧設定(company weight 40 / threshold 50)でも完全一致は similar」「ウェイトを0にしても完全一致は similar のまま」 / `duplicate-flow.spec.ts` 「company weight 40 / threshold 50 に変えても similar のまま」 |
| 簡易照会は企業名だけで実行可能 | `duplicate-flow.spec.ts` 「簡易照会は企業名だけで実行でき、電話番号に必須マークがない」 |
| 簡易照会の電話番号に必須マークなし | 同上(`.req` が0件・`.opt` が1件であることを確認) |
| 営業予定登録の電話番号に必須マークあり | `duplicate-flow.spec.ts` 「4項目を営業予定登録へ引き継ぎ、そこでは電話番号とWebサイトが必須になる」 |
| 4入力を営業予定登録へ引継ぎ | 同上 |
| 引継ぎ後の編集値で再判定 | `duplicate-flow.spec.ts` 「引継ぎ後に企業名を編集すると、その値で再判定される」 / `duplicate.test.ts` 「照会でclearでも、編集後の値では別の判定になりうる」 |
| Reservedはreserved | `duplicate.test.ts` 「Reserved案件に一致したら reserved」 / `seed.test.ts` 「Reserved企業は reserved」 / `duplicate-flow.spec.ts` 「Reserved企業は営業不可、有効受注企業も営業不可」 |
| 有効受注はordered | `duplicate.test.ts` 「保護期限内の受注案件に一致したら ordered」 / `seed.test.ts` 「有効受注の企業は ordered」 |
| 期限切れ企業名一致はsimilar | `duplicate.test.ts` 「保護期限切れの通常案件でも、企業名が一致すれば similar」「保護期限切れの受注案件は ordered にせず similar にする」 / `duplicate-flow.spec.ts` 「保護期限切れの企業名一致は重複の可能性ありになる」 |
| 旧設定・法人格差・期限切れ通常案件・期限切れ受注案件のドメインテスト | `duplicate.test.ts` の §4.4 / §4.5 ブロック、`normalize.test.ts` 全体 |

追加で検証していること:

- 法人格・全角半角・記号の違いの吸収(`normalize.test.ts`、`audit-fixes.test.ts` #1)
- 電話番号の国際表記・内線表記の吸収(`normalize.test.ts`)
- 単一ラベル・不正ホスト名の拒否(`normalize.test.ts`)
- Reservedと有効受注が競合したときの優先順位(`duplicate.test.ts`)
- 施設が違うときは即時停止にしない(`duplicate.test.ts` 「施設単位の扱い」)
- 営業不可になった案件を候補から外す(`duplicate.test.ts`、`review-notify.spec.ts`)

## §21.2 商品マスター

| 仕様の項目 | 検証しているテスト |
| --- | --- |
| 販売中商品だけ営業予定登録へ表示 | `products.spec.ts` 「販売中の商品だけが営業予定登録に出る」 / `products.test.ts` 「販売中だけを商品名で一意化して返す」 |
| 停止商品は新規入力へ非表示 | `products.spec.ts` 「停止中にすると新規入力から消え、過去案件では表示が続く」 |
| 販売中商品を追加すると登録・詳細へ自動反映 | `products.spec.ts` 「本部が販売中商品を追加すると、登録画面と案件詳細へコード修正なしで反映される」 |
| 過去案件の停止商品は表示継続 | `products.spec.ts` 「停止中にすると新規入力から消え、過去案件では表示が続く」 / `products.test.ts` 「停止商品でも保存済みの行は残す」 / `audit-fixes.spec.ts` 「停止中の商品の金額は保存しても消えない」 |
| 過去金額履歴の商品名は表示継続 | `products.spec.ts` 「過去の金額履歴に残った停止商品の名前は変わらない」 / `dealOps.test.ts` 「追加受注時の商品名を履歴に残す」 |
| 案件検索が商品名で動作 | `products.spec.ts` 「案件検索が商品名で動く」 |
| 追加受注が販売中商品名を参照 | `products.spec.ts` 「追加受注は販売中の商品名を参照する」 |

追加で検証していること:

- 停止した商品を再開すると新規入力へ戻る(`products.spec.ts`)
- 停止中の商品は案件詳細でも入力できない(`audit-fixes.spec.ts`)
- 商品画像の形式・容量の検証と代替画像(`products.test.ts`、`products.spec.ts`)

## §21.3 審査・通知

| 仕様の項目 | 検証しているテスト |
| --- | --- |
| similarだけが本部審査へ入る | `review-notify.spec.ts` 「重複審査へ入るのは similar だけ」 / `seed.test.ts` 「重複審査待ちは similar 判定の案件だけ」 |
| 承認はメッセージなしで実行可能 | `review-notify.spec.ts` 「承認はメッセージなしで実行でき、営業不可と差し戻しは必須」 |
| 営業不可・差し戻しはメッセージ必須 | 同上 |
| 登録者本人だけに通知 | `review-notify.spec.ts` 「審査結果は申請した本人だけに通知され、同僚や既存担当者には届かない」 / `permissions.test.ts` 「宛先が自分のときだけ見える」 |
| 同代理店の他ユーザーへ通知しない | 同上(代理店管理者で確認) |
| 既存担当者へ通知しない | 同上(既存案件の担当者で確認) |
| 申請者へ既存案件の機密情報を表示しない | `review-notify.spec.ts` 「申請者には既存案件の機密情報を見せない」 |
| 再読み込み後も審査・通知・履歴を維持 | `review-notify.spec.ts` 「再読み込みしても審査・通知・履歴が残る」 |

追加で検証していること:

- 比較画面に「既に登録されている情報」と「今回新しく登録された情報」が並び、一致項目が強調される(`review-notify.spec.ts`)
- 正規化値は主要項目ではなく「判定の詳細」に入る(同上)
- 審査すると案件・申請履歴・通知・監査ログがそろって更新される(`review-notify.spec.ts` §14.4)
- 申請履歴で「重複判定」と「審査結果」を分けて表示する(`audit-fixes.spec.ts`)

## §21.4 既存機能の回帰

| 仕様の項目 | 検証しているテスト |
| --- | --- |
| 保護期間 | `protection.test.ts`(14件) / `dealOps.test.ts` §10.7 / `regression.spec.ts` 「見積を入れると見積提出になり、受注を入れると受注確定で365日になる」「§10.7 基本設定の変更を既存案件へ反映できる」 / `audit-fixes.test.ts` #2 #4 #5 |
| 追加受注 | `dealOps.test.ts` §13(4件) / `regression.spec.ts` 「追加受注は初回受注を上書きせず、保護期限を受注日から365日にする」 / `audit-fixes.spec.ts` 「追加受注の受注日を空にすると登録できない」 |
| 案件進捗 | `dealOps.test.ts` §11 / `regression.spec.ts` 「案件進捗・営業活動の3パターンが保存でき、保護期限が更新される」 |
| 営業活動 | 同上 |
| ロール権限 | `permissions.test.ts`(20件) / `regression.spec.ts` 「ロール権限」3件・「§9.3 代理店では所属代理店・案件ID・登録者を出さない」 |
| ダッシュボード | `dashboard.test.ts`(21件) / `regression.spec.ts` 「カードの件数と内訳一覧の行数が一致する」「集計範囲がロールで変わる」 |
| コンソールエラーなし | `regression.spec.ts` 「主要画面でコンソールエラーが出ない」(本部13画面 + 代理店9画面を巡回) |
| モバイル横方向オーバーフローなし | `mobile.spec.ts`(8件、Pixel 7エミュレーション) |

## §19 レスポンシブ(モバイル)

| 仕様の項目 | 検証しているテスト |
| --- | --- |
| 代理店側の主要画面が操作可能 | `mobile.spec.ts` 「代理店側の主要画面が横スクロールなしで表示される」(9画面) |
| 主要操作に横スクロールが不要 | `mobile.spec.ts` 「案件一覧は横スクロールではなく縦積みで表示される」 |
| ボタンが画面外へ出ない | `mobile.spec.ts` 「主要な操作ボタンが画面内に収まっている」 |
| 入力欄が重ならない | `mobile.spec.ts` 「入力欄が重ならない」 |
| 案件詳細の並び順(基本情報→保護情報) | `mobile.spec.ts` 「案件詳細は基本情報→保護情報の順に縦に並ぶ」 |
| メニュー・モーダル | `mobile.spec.ts` 「メニューは開閉でき、開いた状態でも画面からはみ出さない」「モーダルも画面内に収まる」 |

## 初期データの整合

`seed.test.ts`(21件)と `audit-fixes-2.test.ts` の「初期データの整合(2巡目の指摘)」で、初期データが仕様の前提を満たすことを確認しています。

- 案件IDの重複なし / 全案件が保護期限を持つ / 受注確定には受注イベントがある
- 受入テストの基準になる4案件(東都ホテル・有効受注・期限切れ通常・期限切れ受注)がそろっている
- 販売中10件・停止中2件で、停止中の商品が過去案件に残っている
- 通知の宛先が必ず申請者本人 / 営業不可・差し戻しの審査にメッセージがある

## レビュー指摘の回帰

Codexによる仕様突き合わせレビューを2巡おこない、見つかった不具合をすべて直したうえで、二度と戻らないよう固定しています。

| レビュー | 指摘 | 回帰テスト |
| --- | --- | --- |
| 1巡目 | 8件(正規化・受注日・日付検証・再計算の起点・停止商品・申請履歴の表示・金額検証) | `audit-fixes.test.ts`(16件) / `audit-fixes.spec.ts`(5件) |
| 2巡目 | 16件(受注取消後の状態・二重確定・引継ぎの競合・企業単位の内訳・企業詳細の履歴・初期データの整合・弱いテスト5件) | `audit-fixes-2.test.ts`(17件) / `audit-fixes-2.spec.ts`(9件) |
| 3巡目 | 14件(登録完了画面の権限漏れ・受注イベントの起点・完全一致候補の欠落・バックアップの上書き・延長申請の閲覧範囲・キーワード検索・弱いテスト5件) | 同上に追加 |

3巡目の主な指摘:

- **登録完了画面が案件IDを直接入れると他代理店の情報を出していた**(§2.4 / blocking)
- 取消済みの初回受注や過去日の追加受注があると、保護期限の起点が最新イベントにならなかった(§10.5 / §13.2)
- 企業名の完全一致で拾った候補が、スコアの下限で比較画面から消えることがあった(§14.1)
- 同じバージョンのバックアップが上書きされていた(§20.1)
- 一般ユーザーの延長申請一覧に、同僚の申請理由まで出ていた(§2.3)
- キーワード検索が、履歴にだけ残る商品名を拾えなかった(§8.2)

2巡目・3巡目では既存テストのうち「不具合があっても通ってしまう」計10件も指摘され、次のように締め直しました。

| 締め直したテスト | 何が甘かったか |
| --- | --- |
| `review-notify.spec.ts` 「重複審査へ入るのは similar だけ」 | reservedも成功扱いにしていたため、初期データの違反を検出できなかった |
| `dashboard.test.ts` 「カードの件数と一覧の行数が一致する」 | 有効受注企業だけ「以上」で比較しており、1社2案件のズレを見逃した |
| `audit-fixes.spec.ts` 「停止中の商品の金額は保存しても消えない」 | 金額が消えても別の「円」を拾って成功していた |
| `audit-fixes.spec.ts` 「重複判定と審査結果を分けて表示する」 | 行全体で検索しており、同じセルに両方出す旧バグでも成功した |
| `audit-fixes.test.ts` 「日付の検証」 | 余分な文字が付いた日付と、金額あり＋日付nullの組み合わせを試していなかった |
| `audit-fixes-2.test.ts` 「有効な最新受注が基準になる」 | 保存結果ではなく、別途呼んだ計算関数の結果だけを見ていた |
| `duplicate.test.ts` 「ウェイト0でも完全一致はsimilar」 | 判定だけを見ており、比較候補が空でも通った |
| `audit-fixes-2.spec.ts` 「バックアップ」 | 既存バックアップがある状態を試していなかった |
| `audit-fixes-2.spec.ts` 「受注取消」 | 期限が「変わったこと」しか見ておらず、正しい日付かを見ていなかった |
| `products.spec.ts` 「案件検索が商品名で動く」 | 件数の範囲しか見ておらず、無関係な案件が混ざっても通った |

内容は `implementation-notes.md` と併せて参照してください。
